# Official and developer-default builds target the portable architecture
# baseline. Hot proof kernels opt into AVX2/AVX-512/PMULL locally and are
# selected at runtime. Benchmarks may still set `RUSTFLAGS=-Ctarget-cpu=native`
# explicitly when measuring one reference machine.
